import seaborn as sns
import pandas as pd

df = pd.DataFrame({
    'category': ['A', 'B', 'C'],
    'values': [10, 20, 15]
})
sns.barplot(x='category', y='values', data=df)
plt.title('Bar Chart: Category Comparison')
plt.show()
